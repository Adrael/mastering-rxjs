import { RouterLinkDirective } from './router-link.directive';

describe('RouterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterLinkDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
